/* GLOBAL */
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #000;
    color: #fff;
}

/* HERO */
.hero {
    height: 90vh;
    background: radial-gradient(circle at center, #111 0%, #000 80%);
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
}

.hero-logo {
    max-width: 250px;
    animation: glow 3s infinite alternate;
}

@keyframes glow {
    0% { filter: drop-shadow(0 0 5px #0033ff); }
    100% { filter: drop-shadow(0 0 20px #cc0000); }
}

.hero h1 {
    font-size: 3rem;
    margin: 10px 0;
    text-shadow: 0 0 10px #0033ff;
}

.call-btn {
    display: inline-block;
    padding: 12px 25px;
    margin-top: 20px;
    background: #cc0000;
    border-radius: 5px;
    color: white;
    font-size: 1.1rem;
    text-decoration: none;
    box-shadow: 0 0 10px #cc0000;
}

/* USLUGE */
.services {
    padding: 40px 20px;
}

.service-item {
    background: #111;
    padding: 20px;
    margin-bottom: 15px;
    border-left: 4px solid #0033ff;
    border-radius: 5px;
}

/* GALERIJA */
.gallery {
    padding: 40px 20px;
    text-align: center;
}

.gallery .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 15px;
}

.gallery img {
    width: 100%;
    border-radius: 8px;
    border: 2px solid #111;
    max-height: 300px;
    object-fit: cover;
}

/* KONTAKT */
.contact {
    padding: 40px 20px;
    background: #111;
    text-align: center;
}

/* FOOTER */
footer {
    text-align: center;
    padding: 15px;
    background: #000;
    font-size: 0.9rem;
}